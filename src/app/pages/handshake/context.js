"use client"

import React from 'react';

const ContextData = React.createContext();

// const [contextData, setContextData] = React.useState({ isReserved: false, docId: null, userIP: '0.0.0.0' }); 
// const contextIsReservedDoc = React.createContext({ contextData, setContextData });

export { ContextData };

